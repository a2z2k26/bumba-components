## What

Brief description of the change.

## Why

The motivation — what problem does this solve or improve?

## Package(s) affected

List the primitives or systems touched (e.g. `@bumba/rate-limiter`, `@bumba/ai-gateway`).

## Checklist

- [ ] Smoke test passes (`npm run test:smoke`)
- [ ] Workspace tests pass (`npm test`)
- [ ] No secrets or credentials in diff
- [ ] README updated if public API changed
